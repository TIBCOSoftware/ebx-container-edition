{{/*
  * Copyright Cloud Software Group, Inc. 2000-2023. All rights reserved.
  */}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: "{{ .Values.global.namespace}}"
  name: "{{ .Values.ebx.prefix}}-ingress"
  annotations:
  {{- if .Values.ingress.annotations }}
  {{- toYaml .Values.ingress.annotations | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: "{{ .Values.ingress.className}}"
  {{- if .Values.ingress.tlsSecret }}
  tls:
    - hosts:
        - "{{ .Values.global.hostname}}"
      secretName: "{{ .Values.ingress.tlsSecret}}"
  {{- end }}
  rules:
    {{ if eq .Values.ingress.hostRuleDefined "true" }}
      - host: "{{ .Values.global.hostname}}"
        http:
    {{ else }}
      - http:
    {{ end }}
          paths:
            - path: "/{{ .Values.ebx.prefix}}/ebx.*"
              pathType: Prefix
              backend:
                service:
                  name: "{{ .Values.ebx.prefix}}-service"
                  port:
                    number: 8080